/* brute force approach:
 * Start at the right hand side of the array
 * and compare ever element before it. If the
 * element is smaller, do nothing. If bigger,
 * increment the `ans` by one. Repeat for every
 * element.
 */

